# EN-019 — Configurar Archivo .env con Variables de Entorno

| Campo | Valor |
|---|---|
| Tipo | Habilitador Técnico |
| Sprint | 1 |
| SP | 2 |
| Estado | ✅ Done |
| Fecha | 2026-05-22 |

## Resumen

`.env.example` versionado con todas las variables clave del sistema (sin valores secretos). `.env` real generado localmente y excluido de Git.

## Variables definidas

| Grupo | Variables |
|---|---|
| PostgreSQL | `POSTGRES_USER`, `POSTGRES_PASSWORD`, `POSTGRES_DB`, `POSTGRES_HOST`, `POSTGRES_PORT`, `DATABASE_URL` |
| MinIO | `MINIO_ROOT_USER`, `MINIO_ROOT_PASSWORD`, `MINIO_ENDPOINT`, `MINIO_BUCKET`, `MINIO_SECURE` |
| JWT | `JWT_SECRET`, `JWT_ALGORITHM`, `JWT_EXPIRES_HOURS`, `JWT_RESET_EXPIRES_HOURS` |
| App | `APP_ENV`, `APP_CORS_ORIGINS` |

## Archivos

- [MangoVision/.env.example](../../../../.env.example) — versionado en Git, plantilla.
- `MangoVision/.env` — local, listado en `.gitignore`, copia del example con valores de desarrollo.

## Cómo probar

```powershell
Test-Path "c:\Users\Patrick Isla\Desktop\notion\MangoVision\.env"        # True
Test-Path "c:\Users\Patrick Isla\Desktop\notion\MangoVision\.env.example" # True
git -C "c:\Users\Patrick Isla\Desktop\notion\MangoVision" check-ignore .env  # devuelve .env (ignorado)
```

## Criterios verificados

- [x] `.env.example` versionado sin valores sensibles reales.
- [x] `.env` ignorado por Git.
- [x] Variables agrupadas y comentadas por contexto.
- [x] `DATABASE_URL` con driver `psycopg` (SQLAlchemy 2.x compatible).
- [x] `JWT_SECRET` documentado para regenerarse en producción.

## Notas técnicas

- `JWT_EXPIRES_HOURS=8` cumple HU-004 (sesión 8 h).
- `JWT_RESET_EXPIRES_HOURS=2` cumple HU-006 (link reset 2 h).
- `APP_CORS_ORIGINS=http://localhost:5173` apunta al puerto default de Vite.
